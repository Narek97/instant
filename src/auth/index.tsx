// import { useNavigate, useSearchParams } from "react-router-dom";

const Auth = () => {
  // const [queryParameters] = useSearchParams();

  // const { isLoading: isLoadingGenerateToken, error: errorGenerateToken } =
  //   useGenerateTokenQuery<GenerateTokenQuery, Error>(
  //     {
  //       generateTokenArgs: { code },
  //     },
  //     {
  //       onSuccess: (request) => {
  //         setCookies(TOKEN_NAME, request.generateToken.token);
  //         navigate("/workspaces");
  //       },
  //     },
  //   );
  //
  // if (errorGenerateToken) {
  //   return <CustomError error={errorGenerateToken.message} />;
  // }
  //
  // if (isLoadingGenerateToken) {
  //   return <CustomLoader />;
  // }

  return <></>;
};

export default Auth;
